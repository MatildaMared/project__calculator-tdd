export const NUMBERS = [1, 2, 3, 4, 5, 6, 7, 8, 9, 0];

export enum Operator {
	ADD = "+",
	SUBTRACT = "-",
	MULTIPLY = "*",
	DIVIDE = "/",
}

export const OPERATORS: Operator[] = [
	Operator.ADD,
	Operator.SUBTRACT,
	Operator.MULTIPLY,
	Operator.DIVIDE,
];
